<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hotel">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".FeedbackDetails"></activity>
        <activity android:name=".Feedback_List" />
        <activity android:name=".CrewFeedback" />
        <activity android:name=".CrewDashboard" />
        <activity android:name=".HotelDetails" />
        <activity android:name=".CrewDetails" />
        <activity android:name=".HotelList" />
        <activity android:name=".Hotel_Register" />
        <activity android:name=".CrewList" />
        <activity android:name=".Crew_Register" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>